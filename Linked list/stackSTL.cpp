#include<iostream>
#include<stack>
using namespace std;


int main(){
/*
// operations on STACK

    // creation of stack
    stack<int> s;

//  PUSH or insertion in a stack
    for(int i=0;i<3;i++){
        int data;
        cout<<"Enter a data no. "<<i<<endl;
        cin>>data;
        s.push(data);
    }
    // TOP function usage
    cout<<"printing top element is: "<<s.top()<<endl;

    // EMPTY function usage
    if(s.empty()){
        cout<<"Stack is empty"<<endl;
    }
    else{
        cout<<"Stack is not empty"<<endl;
    }
    // size function usage
    cout<<"Stack size is: "<<s.size()<<endl; 
*/

    return 0;
}